

table
  - @tiles.each_slice(8) do |row|
    tr
      - row.each do |cell|
          <td><div class='game_tile' id="#{cell._id}">#{cell.name}</div></td>
.done_holder
  a.btn.btn-default.btn-primary.btn-lg#done type="button" href="/top_10"  I'm done!